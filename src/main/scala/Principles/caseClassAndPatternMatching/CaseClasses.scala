package Principles

//case class CaseClasses(name: String, value: Int)
//// The constructor parameter are public and immutable
//
//val a = CaseClasses("Hello", 1)

// Here the parameters are immutable so we can't do
// a.name = "New" //Error

// Case class can be defined with var - but it shouldn't be
//case class NotRecommended(var name: String)
//val b = NotRecommended("Hello!")
//b.name = "New" //This works

// ------------------------------------------------------------ //









